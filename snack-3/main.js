// Il software deve chiedere per 10 volte all’utente di inserire un numero. 
// Il programma stampa la somma di tutti i numeri inseriti.

// 1. Creo variabile dove inserire i numeri da sommare e definisco tipologia
let numero = Number
console.log(numero)

// 2. creo ciclo for per ripetere l'operazione n volte
for (i = 0; i <= 3; i++){
    numero = prompt("Inserisci un numero")
}

// 3. scrivo risultato in console.
console.log(numero + numero + numero + numero + numero + numero + numero + numero + numero + numero)